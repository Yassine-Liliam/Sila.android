package app.silati.ui.theme

import androidx.compose.ui.graphics.Color

// Fallback palette, seeded from Silati's brand cyan (#22d3ee).
// ponytail: only Android 8–11 ever sees these — 12+ gets dynamic color from the
// wallpaper. Generate a full tonal palette with the Material Theme Builder if the
// fallback ever looks off on an old device.
val Cyan80 = Color(0xFF5DD8EE)
val CyanGrey80 = Color(0xFFB0CBD2)
val Teal80 = Color(0xFF9FD4C7)

val Cyan40 = Color(0xFF00687A)
val CyanGrey40 = Color(0xFF4A6269)
val Teal40 = Color(0xFF2F6A5D)
